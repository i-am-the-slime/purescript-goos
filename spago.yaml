package:
  name: goos
  dependencies:
    - effect
    - prelude
    - either
    - maybe
    - arrays
    - bifunctors
    - control
    - exceptions
    - unsafe-coerce
    - yoga-json
    - st
    - http-methods
    - ordered-collections
    - nullable
    - foldable-traversable
    - functions
  build:
    strict: true
    pedantic_packages: true

workspace:
  package_set:
    registry: 49.0.0
  backend:
    cmd: echo "This package uses purescript-native Go backend"
    dependencies: []